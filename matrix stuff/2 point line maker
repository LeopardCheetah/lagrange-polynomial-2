# given two points, find a line that crosses both

xone, yone = input('integers only, please enter the x and y of the first point\n').split()
xtwo, ytwo = input('integers only, please enter the x and y of the second point\n').split()

slopenum = (int(ytwo) - int(yone))
slopedenom = (int(xtwo) - int(xone))

const = int(ytwo) - int(xtwo)*(slopenum/slopedenom)

print('y =', slopenum, '/', slopedenom, 'x + (', const, ')')

# this is not at all a good program
